import type { NextConfig } from 'next';
console.log("BUILD TIME CHECK: NEXT_PUBLIC_API_URL is", process.env.NEXT_PUBLIC_API_URL);
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
